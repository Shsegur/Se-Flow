rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite leer y escribir a la aplicación si la autenticación anónima está activa
      allow read, write: if request.auth != null;
    }
  }
}
